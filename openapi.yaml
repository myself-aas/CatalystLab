openapi: 3.1.0
info:
  title: CatalystLab API
  version: '1.0.0'
servers:
  - url: http://localhost:4010
paths:
  /hello:
    get:
      summary: Hello endpoint
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: 'Hello, CatalystLab!'
